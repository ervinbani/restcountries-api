{
  "name": "rest-countries-api",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "build": "npm run build:ts && npm run build:sass && npm run copy:html",
    "build:ts": "tsc",
    "build:sass": "sass src/styles/main.scss docs/styles/main.css",
    "copy:html": "powershell -Command \"Copy-Item src/*.html docs/ -Force\"",
    "watch:ts": "tsc --watch",
    "watch:sass": "sass src/styles/main.scss docs/styles/main.css --watch",
    "dev": "npm run watch:ts & npm run watch:sass",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "sass": "^1.94.2",
    "typescript": "^5.6.0"
  }
}
