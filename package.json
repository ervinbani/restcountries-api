{
  "name": "rest-countries-api",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "build": "tsc && sass src/styles/main.scss public/styles/main.css",
    "watch:ts": "tsc --watch",
    "watch:sass": "sass src/styles/main.scss public/styles/main.css --watch",
    "dev": "npm run watch:ts & npm run watch:sass",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "sass": "^1.94.2",
    "typescript": "^5.6.0"
  }
}
